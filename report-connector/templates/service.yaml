apiVersion: v1
kind: Service
metadata:
  name: {{ include "report-connector.fullname" . }}
  labels:
    {{- include "report-connector.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "report-connector.selectorLabels" . | nindent 4 }}
