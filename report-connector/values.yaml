# Default values for report-connector.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: observertc/report-connector
  pullPolicy: Always
  # Overrides the image tag whose default is the chart appVersion.
  tag: "0.1"

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""

podAnnotations: {}

podSecurityContext: {}
# fsGroup: 2000

securityContext: {}
# capabilities:
#   drop:
#   - ALL
# readOnlyRootFilesystem: true
# runAsNonRoot: true
# runAsUser: 1000

service:
  type: ClusterIP
  port: 27788

ingress:
  enabled: false
  annotations: {}
  # kubernetes.io/ingress.class: nginx
  # kubernetes.io/tls-acme: "true"
  hosts:
    - host: chart-example.local
      paths: []
  tls: []
  #  - secretName: chart-example-tls
  #    hosts:
  #      - chart-example.local

resources: {}
# We usually recommend not to specify default resources and to leave this as a conscious
# choice for the user. This also increases chances charts run on environments with little
# resources, such as Minikube. If you do want to specify resources, uncomment the following
# lines, adjust them as necessary, and remove the curly braces after 'resources:'.
# limits:
#   cpu: 100m
#   memory: 128Mi
# requests:
#   cpu: 100m
#   memory: 128Mi

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 100
  targetCPUUtilizationPercentage: 80
  # targetMemoryUtilizationPercentage: 80

nodeSelector: {}

tolerations: []

affinity: {}

extraConfigMaps: []
# - name: name-inside-deployment
#   configMapName: configMapName 
#   mountPath: /etc/config2

extraSecrets: {}
#  - name: my-credentials
#    secretName: secret-credential-name
#    mountPath: /etc/credentials

# Additional configurations for micronaut
# remove the {} when you add a configuration. Example configuration below
micronaut: {}
#  mountPath: /etc/config
#  config:
#    pipelines:
#      corePoolSize: 10
#      maxParallel: 50
#      files:
#        - "/etc/config/generalPipeline.yaml"

# pipelines to be loaded
# remove the {} when you add a configuration, example configuration below
pipelines: {}
#  generalPipeline.yaml:
#    name: "General pipeline to dump reports from webrtc-observer"
#    source:
#      type: KafkaSource
#      config:
#        topic: "reports"
#        properties:
#          bootstrap.servers: my-kafka-address:9092
#          group.id: "my-general-consumer-group"
#    evaluator:
#      bufferThresholdNum: 10000
#      bufferThresholdInS: 30
#    sink:
#      type: BigQuerySink
#      config:
#        credentialFile: "/etc/credentials/credential.json"
#        projectId: "myProject"
#        datasetId: "myDataset"
#        schemaCheck:
#          enabled: True
#          createTableIfNotExists: True
#          createDatasetIfNotExists: True
    
